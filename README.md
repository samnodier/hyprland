# hyprland config

I use fira code nerd font

## how to clone this repo and set up

1. clone the repository git clone https://github.com/samnodier/hyprland.git
2. move the configuration to your `.config` `mv hyprland ~/.config`
3. restart
